You are working on the IUH_Exchange_BE project - a Node.js microservices platform for campus exchange at IUH university. The project root is the current directory.

## YOUR TASK (do ALL of these in order):

### Phase 1: Analysis and README
1. Read and understand the entire project structure (packages/*, frontend/*, infra/*, docker-compose.yml, all config files)
2. Create a comprehensive README.md at the project root covering:
   - Project overview and architecture diagram (mermaid)
   - Tech stack details
   - Prerequisites and setup instructions
   - How to run locally (docker-compose plus npm scripts)
   - API documentation summary (all endpoints per service)
   - Environment variables needed (reference .env.example)
   - Testing instructions
   - Deployment guide
   - Complete project structure tree

### Phase 2: Code Refactoring and Error Fixing
1. Review ALL source files in packages/*/src/ and frontend/src/
2. Look for: code smells, missing error handling, inconsistent patterns, security issues, performance issues
3. Fix any bugs you find
4. Add proper JSDoc comments to all exported functions
5. Ensure consistent error handling patterns across all services
6. Add input validation where missing using Zod or express-validator
7. Ensure all async routes have proper try-catch error handling with next(err)
8. Look for any TypeScript errors in frontend and fix them

### Phase 3: Unit Tests with Jest
1. Set up Jest at the root level as the test runner for the monorepo
2. Create jest.config.js at root and in each package
3. Install required test dependencies: jest, @jest/globals, supertest, mongodb-memory-server (for integration tests if feasible)
4. Write comprehensive unit tests for EVERY service:
   - common: test helpers (hashPassword, comparePassword, compareToken, parsePagination), ApiResponse, PageResponse, cache module
   - user-service: test auth.controller (register, verifyOtp, login, refreshToken, changePassword, forgotPassword, resetPassword, logout), user.controller (getMyProfile, getUserProfile, updateProfile), admin.controller (listUsers, updateUserRole, adjustKarma, toggleBanUser)
   - product-service: test product.controller (listProducts, createProduct, updateProduct, deleteProduct, searchProductsHandler, getMyProducts, getProductById, getUploadUrl, getPendingProducts, resolveProduct, getProductStats)
   - order-service: test order.service (createOrder with idempotency, confirmOrder, rejectOrder, cancelOrder, getOrders, getOrderById)
   - chat-service: test chat.controller (getUserConversations, getConversationHistory, markConversationAsRead, markAllConversationsAsRead, searchMessages)
   - notification-service: test notification.controller (getNotifications, getUnreadCount, markAsRead, markAllAsRead, deleteNotification)
   - lost-found-service: test lostfound.controller (listItems, getItemById, createItem, updateItem, deleteItem, claimItem)
   - api-gateway: test middleware (auth-filter, circuit-breaker state machine)
5. Mock external dependencies properly:
   - MongoDB with jest.mock for Mongoose models
   - Redis with jest.mock for getRedis
   - Kafka with jest.mock for createProducer/createConsumer
   - S3 with jest.mock for presigned URL generation
   - ElasticSearch with jest.mock for search
6. Create test setup files with common mocks
7. Add test scripts to each package.json and root package.json
8. Target at least 70 percent statement coverage for each service

### Phase 4: Verification
1. Run all tests and fix any failures
2. Ensure the project builds without errors

### RULES:
- Do NOT modify docker-compose.yml, Dockerfiles, or .env files
- Do NOT delete any existing functionality
- Keep all existing code that works - only improve, fix, or add
- Use the existing project conventions (Express, Mongoose, common package imports with @iuh-exchange/common)
- Commit your changes with descriptive messages after each phase
